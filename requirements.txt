# Pinned from `pip freeze` in the environment that produced every committed
# result on 2026-09-16 (Python 3.11.14, macOS arm64). Only the packages the
# code imports, plus what is needed to execute the notebooks; their transitive
# dependencies are resolved by pip. The full freeze is not kept.
#
# Python: 3.11 (3.10+ required: microns_datacleaner uses `match` statements).

# --- imported by scripts/ and notebooks/ ---
numpy==2.0.2
pandas==2.3.3
matplotlib==3.9.4
scikit-learn==1.6.1          # PCA, silhouette, logistic regression
h5py==3.14.0                 # direct reads of microns_functional.h5
plotly==6.7.0                # interactive 3-D figures (write_html)
seaborn==0.13.2              # eda_preproc.ipynb only
Microns-DataCleaner==0.2.1.7 # MicronsFunctionalReader + the H5 downloader (PyPI name)

# --- needed to execute the notebooks (not imported by project code) ---
jupyterlab==4.6.3
ipykernel==7.3.0
nbclient==0.11.0
nbformat==5.11.1
kaleido==1.4.0               # plotly static export (experiment1: v1_3d_*.svg/png).
                             # kaleido >= 1.0 needs a Chrome/Chromium; run
                             # `python -c "import plotly.io as pio; pio.get_chrome()"` once.

# --- scripts/convert_svgs.py only (SVG -> PDF for the report figures) ---
CairoSVG==2.9.1
